This project is a full-stack invoice management system for small businesses. It lets users create GST invoices with dynamic product rows, automatic total and amount-in-words calculation, and print-ready output (PDF via browser print). It also supports customer auto-fill by mobile number, sequential invoice numbers (e.g., IN001), and complete invoice lifecycle management (create, list, edit, and delete) through a React frontend and Express/MongoDB backend.
